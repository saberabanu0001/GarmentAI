export default function DocumentLibraryPage() {
  return (
    <div className="mx-auto max-w-3xl rounded-xl bg-white p-8 shadow-md">
      <h1 className="text-xl font-bold text-zinc-900">Document library</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Placeholder route. Connect uploads and per-factory collections here (Chroma
        / object storage). Content stays out of components — use API modules when
        you implement it.
      </p>
    </div>
  );
}
